{
  "name": "Declare",
  "env": "production",
  "version": "0.0.1",
  "description": "Common package containing various configuration common to client and app",
  "main": "index.js",
  "repository": "https://github.com/jaredloomis/Declare",
  "author": "Jared Loomis <jaredloomis@protonmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "./scripts/build.sh",
    "start": "cd executor && yarn run start && cd ../server && yarn run start",
    "restart": "cd executor && yarn run restart && cd ../server && yarn run restart",
    "stop": "cd executor && yarn run stop && cd ../server && yarn run stop"
  },
  "dependencies": {
    "yarn": "^1.22.21"
  },
  "devDependencies": {
    "yarn": "^1.22.21"
  }
}
